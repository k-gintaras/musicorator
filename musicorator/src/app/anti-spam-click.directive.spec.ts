import { AntiSpamClickDirective } from './anti-spam-click.directive';

describe('AntiSpamClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AntiSpamClickDirective();
    expect(directive).toBeTruthy();
  });
});
